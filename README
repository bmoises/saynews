#Saynews
Uses OSX's built in say command to read news articles from the terminal.

#Usage
ruby bin/saynews.rb ARTICLE_URL OPTIONS

* ARTICLE_URL: is an article url for sites supported(see lib/sites)
* OPTIONS: -r (resume playback)
